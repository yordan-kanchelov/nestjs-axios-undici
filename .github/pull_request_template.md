## What changes

<!-- What this PR does and why. -->

## Checklist

- [ ] `npm test` passes (lint runs in CI too: `npm run lint`)
- [ ] A changeset (`npx changeset`) describes any change to the published package
- [ ] Docs updated if the public API or behaviour changed
